import { Container, Text } from '@chakra-ui/react';

// Test the Container component with variants
export default function TestContainer() {
  return (
    <Container variant='clearTop'>
      <Text>Container with clearTop variant</Text>
    </Container>
  );
}
